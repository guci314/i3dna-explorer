# language: zh-CN
功能: Agent API 验收（不碰界面走完目录体系闭环）
  背景:
    不擅长操作界面的 agent（LLM/脚本/CI）经 i3dna_api.py 与目录体系交互：
    读桥动词出 JSON（判据面），写桥动词直通引擎（账纪律原样）。
    外部 LLM 在边界打桩（fakes/fake_engine.py），暂存-验收-落位-账全链路真跑。

  场景: agent 查询目录树并预检弧
    假如 一棵最小验收树
    当 agent 调 API tree
    那么 JSON 含 域 x域 类 甲 方法 办 与 根场所
    当 agent 调 API 预检任务 办 案卷 c1
    那么 JSON 弧 申请.md 在场 产物弧指向 实例/甲/c1/出.md

  场景: agent 点火并办结（账是判据）
    假如 一棵最小验收树
    当 agent 调 API 桩引擎点火 办 案卷 c1
    那么 产物文件 实例/甲/c1/出.md 在场
    而且 账记 状态 执行 产物清单带 sha256
    当 agent 调 API 办结 办 案卷 c1
    那么 账记 状态 事后追认

  场景: 办结校验门拒绝零副作用
    假如 一棵带拒绝门的验收树
    当 agent 调 API 办结 办 案卷 c1
    那么 退出码非零 账未写

  场景: 老子带手艺先查后答（泰勒主义干不了爱因斯坦的工作）
    假如 一棵带手艺卡的最小验收树
    当 问老子 树里几个任务
    那么 老子回答包含 桩答（查过）：任务 1 个
    而且 老子查过真 API

  场景: 无手艺卡的树老子保持快照问答
    假如 一棵最小验收树
    当 问老子 现在该干嘛
    那么 老子回答包含 桩答：全网新鲜
