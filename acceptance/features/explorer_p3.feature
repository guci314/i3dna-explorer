# language: zh-CN
功能: 探索器客户验收（P3·覆盖报告与视觉基线）
  客户可读可签字的验收场景——判据是断言与树上账目,截图是证据不是判据。
  P3=MBT 红利:任务图即 Petri 网,验收挂树上(变迁/边覆盖=GraphWalker 停机条件);
  P4=视觉契约:范帱色/徽章树视图对 golden 基线验签,变更需人工审基线。

  场景: 覆盖报告挂在树上
    假定 打开目录 trade-v4
    当 点击工具栏动作 覆盖报告
    那么 详情包含 覆盖报告
    而且 详情包含 变迁覆盖
    而且 详情包含 边覆盖
    而且 详情包含 未点火
    而且 截图留证

  场景: 树视图与视觉基线一致
    假定 打开目录 trade-v4
    当 显示窗口
    那么 树视图与基线一致 trade-v4
    而且 截图留证
