# i3dna explorer 使用手册

> 面向操作员。架构与设计裁决见同目录 `ARCHITECTURE.md`（那是给改代码的人
> 看的，本手册只管怎么用）。

## 1. 这是什么

目录树就是软件本身：**类是流程定义，实例是案卷，方法（微任务）是工位**。
explorer 是这棵树的浏览器——左边看树，右边看内容，**右键干活**：预检、
点火、办结、对账、推进，全部经引擎入账，界面上没有一笔旁路操作。

树上画的条件/事件网按**三色谱**着色：

- **绿**＝人工工位（执行者: 人）——机器不代人点火，办结入账
- **蓝**＝LLM 工位——点火时引擎起模型子进程干活
- **红**＝程序工位（有 执行程序/）——点火跑确定性程序

## 2. 启动

```bash
i3x                      # 缺省打开 ~/work/report_generate/md-devloop-m1
i3x <包根目录>            # 指定别的树
```

mac 与 LinuxMint 都装了这个函数（`~/.zshrc` / `~/.bashrc`）。等价直呼：

```bash
python3 ~/work/explorer/i3dna_explorer.py <包根>
```

- 启动器会先把 `ARCHITECTURE.md` 同步到 Obsidian vault 投影（mac 侧）。
- 怀疑崩溃时看终端 traceback 或 `/tmp/i3x.log`。
- 命令行/脚本走引擎 API（读桥 JSON、写桥直通）：
  `python3 ~/work/report_generate/i3dna-engine/i3dna_api.py <动词> <包根> …`

## 3. 界面一览

```
┌────────────────────────────────────────────────────────────┐
│ 包根: xxx │ 引擎: [GLM 5.3 (OMP·ACP)▾] │☐沙盒│ 视角:[目录▾]│ 主体: xxx │
│ 刷新 │ 全树对账 │ 修复提案 │ 覆盖报告 │ 登录 │ 推进 │ 终止      │
├──────────────┬─────────────────────────────────────────────┤
│ 目录树        │  [内容] [执行流] [老子]                        │
│ (或场所视角)  │  详情区：文件预览 / 执行流图 / 对话             │
└──────────────┴─────────────────────────────────────────────┘
```

- 左树两种视角（工具栏「视角」切换）：**目录**＝按 域/类/实例 翻；
  **场所**＝按组织场所看拓扑。
- 详情区三页：**内容**（选中节点的文件/说明）、**执行流**（流程图，
  按案卷分页）、**老子**（跟树对话，见 §8）。

## 4. 工具栏逐项

| 控件 | 作用 |
|---|---|
| 引擎下拉 | 蓝/绿任务点火用的模型。默认 GLM 5.3（OMP·ACP，思考烧穿自动降档重试）；可换 OMP 直连、DeepSeek 两款 |
| 沙盒 | 勾上后点火的产物落隔离沙盒目录，不写真树（试跑用） |
| 视角 | 目录树 / 场所拓扑 切换 |
| 主体 | 当前登录人（凭证＝人员档案）。点开登录/切换 |
| 刷新 | 重读全树（外部改动后手动刷） |
| 全树对账 | lint 报告：悬空引用、账实差、悬账……错误/警告分级 |
| 修复提案 | 对账发现的问题给出修法建议 |
| 覆盖报告 | MBT 变迁/边覆盖：哪些工位没点过火、哪些边没走过 |
| 登录 | 同「主体」 |
| 推进 | converge：自动把使能的蓝/红工位一路点到不动点（绿站只列出，**永不自动点火**） |
| 终止 | 杀掉在途的点火/推进进程 |

## 5. 状态叠加（一眼断病）

选中微任务时：

- **亮蓝**＝使能（输入齐、门全开）；**灰蓝**＝未使能（缺输入或被门拦）
- **⛔** ＝ lint 悬空（引用的目标不存在）
- **⟳** ＝输入过期（上游改过、本站该重跑）
- 选中后血缘高亮：**输入＝淡绿背景，产物槽＝淡橙背景**

## 6. 右键菜单（按节点类型）

**类目录**（有 方法/ 的目录）：
- 实例化… —— 按实例 schema 立新案卷
- 实例结构（实例 schema）—— 看本类案卷该有哪些槽
- 🥚 本类实例（N）—— 一键跳到本类实例架

**过程实例**（案卷节点）：
- 💬 对话（话语即签字）—— 开对话面板，说话即办事（见 §7）
- 推进本实例（案卷号）—— 只推这一个案卷
- 补播字段区（缺键时出现）—— 状态.json 缺槽时一键补
- — 工位 — 本案卷每个方法一行：
  - **办结 ☐**（绿站）：人工完成产物后办结入账（对 uuid 核销单据、
    草稿转正、署名一并落账）
  - **点火 ▶**（蓝/红站）：按工具栏引擎执行

**方法**（定义级，只读面）：
- 🆕 新案卷（立案开工位）
- 预检 / 点火记录 —— 弧解析与历史账
- 渐进符号化（见 §9）
- 登记输入弧 / 登记产物弧

**文件**：
- 运行… —— 直接跑 .py/.sh（可带参数，GUI 程序弹真窗口）

> 调用约定（99号）：**一切点火/办结都是实例侧调用**——方法节点上只留
> 预检/记录等定义级只读面，业务动词去实例右键「工位」。

## 7. 日常操作

**立新案**：类→「实例化…」，或方法→「🆕 新案卷」。

**办绿单**（人工工位）三条路，任选：
1. 实例→工位→**办结 ☐**（产物已写好时直接入账）
2. 双击待办单据 → 通用办理界面（表单式，不落裸编辑器）
3. 实例→**💬 对话**：说话即签字——对话面板无确认按钮，每条话语由模型
   编成 JSON 信封，查询走 api_query 白名单、签字走 api_write 逐条入账；
   会话只留内存，关了就散

**点火蓝/红**：实例→工位→**点火 ▶**。产物落槽/落夹（目录弧产物 uuid
自动起名），账与 journal 由引擎落。勾「沙盒」先试跑。

**推进**：工具栏「推进」＝自动循环（使能的蓝红一站站点到不动点；
空夹门/取值门会物理拦），绿站等人。跑飞了按「终止」。

**对账**：「全树对账」看报告；「修复提案」要建议；改完「刷新」再看。

## 8. 老子页（跟树对话）

详情区第三页。对选中的树提问（"树里几个任务？""报销003 走到哪了？"），
它走只读 API 查树作答；也承担办单助手的机械层（deepseek 直连，不走 omp）。

## 9. 渐进符号化

- **绿站**→「检测可信息化（绿→蓝）」：看人工活能不能交给模型
- **蓝站**→「检测可符号化」「编译（生成符号程序）」「编译验收」：
  模型活的确定性部分能不能固化成程序
- **红站**→「回退联结主义」：程序活交还模型（改需求时过渡）

编译产物先落暂存区待裁决，验收通过才转正——不让机器悄悄改法定程序。

## 10. 登录（主体）

凭证＝**人员档案**（实例/人员/*/凭证.md，pbkdf2 盐化哈希，无明文无
独立账号库）。工具栏「主体」或「登录」，员工编号或姓名＋口令。登录
经引擎 login 入账。谁在办单、谁签的字，账上见人。

## 11. 常见问题

- **点火没反应/排队**：看「终止」旁是否占线提示——一次只跑一个进程；
  「推进」没点绿站是正常的（待人办是合法等待态）。
- **模型报错/烧穿**：默认引擎会自动降档重试；换引擎下拉再试。
- **LinuxMint 上蓝火**：机器没有 omp 时，OMP 款引擎不可用——浏览、
  红火（程序）、办结、对账照常；要蓝火就装 omp 或换 CLI 款引擎。
- **改了树没变化**：点「刷新」（explorer 不监听文件系统）。
- **写操作纪律**：界面上一切写都经引擎（暂存-验收-落位-账）；想绕过
  界面写，走引擎 API 的 draft/fire/settle——树不容账外直写。
