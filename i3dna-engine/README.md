# i3dna-engine

I3DNA 微任务通用执行引擎（写半边；读半边 = ../i3dna-lint）。canonical 在此维护，
部署副本在各包 `_执行程序` 槽（如 `8.5/__创建窗口/_执行程序/.../__微任务执行程序.py`），
复制方向永远 canonical→副本。用法与通用性说明见部署副本旁的 `__执行说明.md`，
或 `python3 i3dna_engine.py --help`。
